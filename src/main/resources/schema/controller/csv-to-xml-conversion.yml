  post:
    tags:
      - csv-to-xml-conversion
    summary: csv to xml convert
    description: |-
      going to convert csv file to xml file
    operationId: csvToXmlConversion
    requestBody:
      content:
        multipart/form-data: # Media type
          schema: # Request payload
            type: object
            properties: # Request parts
              file: # Part 3 (an image)
                type: string
                format: binary
    responses:
      '200':
        description: ''
        content:
          application/json:
            schema:
              type: string